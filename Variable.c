#include <stdio.h>
int main()
{
   int a;    //int형(정수형) 변수 선언
   a = 10;   //변수에 값을 대입
   printf_s("a의 값은 %d \n", a);   //%d: 10진수 정수로 출력 (decimal)
   return 0;    
   /* 0을 반환하여 프로그램이 정상적으로 종료되었다는 걸 알려준다는데 
   아직도 잘 모르겠다. */
}